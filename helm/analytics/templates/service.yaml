apiVersion: v1
kind: Service
metadata:
  name: { { include "soulmatting-analytics.fullname" . } }
  labels: { { - include "soulmatting-analytics.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: http
      protocol: TCP
      name: http
  selector:
    { { - include "soulmatting-analytics.selectorLabels" . | nindent 4 } }
